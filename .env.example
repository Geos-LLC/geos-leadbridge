# Supabase Database
# Get this from: https://app.supabase.com/project/_/settings/database
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:your-password@db.xxxxxxxxxxxxx.supabase.co:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:your-password@db.xxxxxxxxxxxxx.supabase.co:5432/postgres"

# Server
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Encryption
ENCRYPTION_KEY=your-32-character-encryption-key-change-this

# Thumbtack OAuth
THUMBTACK_CLIENT_ID=your-thumbtack-client-id
THUMBTACK_CLIENT_SECRET=your-thumbtack-client-secret
THUMBTACK_REDIRECT_URI=http://localhost:3000/v1/thumbtack/auth/callback
THUMBTACK_WEBHOOK_SECRET=your-thumbtack-webhook-secret

# Sigcore API (app-level connection for phone provisioning, SMS & Instant Call Connect)
SIGCORE_API_URL=https://sigcore-production.up.railway.app
SIGCORE_API_KEY=your-sigcore-api-key

# Instant Call Connect — HMAC secret used to verify incoming Sigcore call-connect webhooks
# Set this to the same value as the "secret" you pass when registering the webhook subscription
SIGCORE_CALL_CONNECT_WEBHOOK_SECRET=your-call-connect-hmac-secret

# Public base URL of this server (used to build webhook callback URLs sent to Sigcore)
APP_BASE_URL=https://your-app-domain.com

# Yelp (for future use)
YELP_API_KEY=your-yelp-api-key
YELP_CLIENT_ID=your-yelp-client-id
YELP_CLIENT_SECRET=your-yelp-client-secret
YELP_WEBHOOK_SECRET=your-yelp-webhook-secret

# Optional: Monitoring & Logging
SENTRY_DSN=
LOG_LEVEL=info

# Redis cache (optional — Railway auto-injects REDIS_URL when plugin is attached)
# When REDIS_URL is unset or CACHE_ENABLED=false, the app falls back to a no-op cache.
REDIS_URL=
CACHE_ENABLED=true
